# Static Frontend Deployment for Biskaken Auto Landing Page + Blog
[variables]
NODE_ENV = "production"
NODE_VERSION = "20"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
    "echo '📦 Installing frontend dependencies...'",
    "npm ci --omit=dev"
]

[phases.build]
cmds = [
    "echo '🏗️ Building static frontend...'",
    "npm run build:frontend"
]

[start]
cmd = "echo '🚀 Serving static files...' && npx serve dist -s -l 5000"