# Static Frontend Deployment for Biskaken Auto Landing Page + Blog
[variables]
NODE_ENV = "production"
NODE_VERSION = "20"
PORT = "3000"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
    "echo '📦 Installing frontend dependencies...'",
    "npm install",
    "echo '🔧 Installing Rollup native dependencies for Linux...'",
    "npm install @rollup/rollup-linux-x64-gnu --save-optional"
]

[phases.build]
cmds = [
    "echo '🏗️ Building static frontend...'",
    "npm run build:frontend",
    "echo '📁 Verifying dist directory...'",
    "ls -la dist/"
]

[start]
cmd = "echo '🚀 Starting static server...' && npx serve -s dist -l 3000"