# Dokploy Backend Environment Configuration
# This file should be used as reference for setting environment variables in Dokploy dashboard

# Server Configuration
NODE_ENV=production
PORT=5000
HOST=0.0.0.0

# Database Configuration (to be set in Dokploy)
DB_HOST=${DB_HOST}
DB_PORT=5432
DB_NAME=biskaken_auto
DB_USER=postgres
DB_PASSWORD=${DB_PASSWORD}

# Security
JWT_SECRET=${JWT_SECRET}

# CORS Origins (production frontend URL)
CORS_ORIGINS=https://biskakenauto.rpnmore.com