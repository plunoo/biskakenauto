# Dokploy Backend Environment Configuration
# This file should be used as reference for setting environment variables in Dokploy dashboard

# Server Configuration
NODE_ENV=production
PORT=5000
HOST=0.0.0.0

# Database Configuration (Real Dokploy PostgreSQL)
DB_HOST=biskakenend-postgres-kbcgia
DB_PORT=5432
DB_NAME=biskaken_auto
DB_USER=postgres
DB_PASSWORD=3coinsltd

# Connection URL for easier configuration
DATABASE_URL=postgresql://postgres:3coinsltd@biskakenend-postgres-kbcgia:5432/biskaken_auto

# Security
JWT_SECRET=biskaken-super-secure-jwt-secret-2026-v5
JWT_EXPIRES_IN=7d

# CORS Origins (production frontend URL)
CORS_ORIGINS=https://biskakenauto.rpnmore.com,https://bisadmin.rpnmore.com

# Demo Mode (set to false for real database)
DEMO_MODE=false