# Dokploy Internal Container Configuration
# This file is optimized for Dokploy container deployment

# === SERVER CONFIGURATION ===
NODE_ENV=production
PORT=5000
HOST=0.0.0.0

# === DATABASE CONFIGURATION ===
# Use Dokploy internal database service
DB_HOST=postgres  
DB_PORT=5432
DB_NAME=biskaken_auto
DB_USER=postgres
DB_PASSWORD=${DB_PASSWORD}
DB_SSL=false

# === SECURITY ===
JWT_SECRET=${JWT_SECRET}

# === CORS CONFIGURATION ===
# Allow both external frontend and internal container communication
CORS_ORIGINS=https://biskakenauto.rpnmore.com,http://localhost:3000,http://frontend:3000

# === DEFAULT ADMIN USER ===
ADMIN_EMAIL=admin@biskaken-v3.com
ADMIN_PASSWORD=admin123
ADMIN_NAME=Admin V3 User

# === API CONFIGURATION ===
API_VERSION=1.0.0
MAX_FILE_SIZE=10485760

# === CONTAINER SPECIFIC ===
# Internal service discovery
FRONTEND_URL=http://frontend:3000
BACKEND_URL=http://backend:5000

# === OPTIONAL SERVICES ===
OPENAI_API_KEY=${OPENAI_API_KEY:-}
GEMINI_API_KEY=${GEMINI_API_KEY:-}
PAYSTACK_SECRET_KEY=${PAYSTACK_SECRET_KEY:-}
TWILIO_ACCOUNT_SID=${TWILIO_ACCOUNT_SID:-}
TWILIO_AUTH_TOKEN=${TWILIO_AUTH_TOKEN:-}

# === LOGGING ===
LOG_LEVEL=info
LOG_FORMAT=combined